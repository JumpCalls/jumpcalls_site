import { createFileRoute, Link } from "@tanstack/react-router";
import { CheckCircle2 } from "lucide-react";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/seo-audit-thanks")({ head: () => ({ meta: [{ title: "SEO Audit Request Received | JumpCalls" }, { name: "robots", content: "noindex" }] }), component: SeoAuditThanks });
function SeoAuditThanks() { return <main className="surface-ink grid-glow flex min-h-screen items-center justify-center px-5 py-20"><div className="w-full max-w-xl rounded-3xl border border-ink-border bg-background p-9 text-center shadow-2xl md:p-12"><CheckCircle2 className="mx-auto size-14 text-primary" /><p className="mt-6 text-xs font-bold uppercase tracking-widest text-primary">Request received</p><h1 className="mt-3 text-3xl font-extrabold md:text-4xl">Your free SEO audit is in the queue.</h1><p className="mt-4 leading-relaxed text-muted-foreground">Thanks for reaching out. The JumpCalls team will review your website and online visibility, then contact you within one business day.</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Button variant="hero" asChild><Link to="/">Back to homepage</Link></Button><Button variant="outline" asChild><a href="https://calendly.com/jumpcalls/60min" target="_blank" rel="noreferrer">Book a strategy call</a></Button></div></div></main>; }
